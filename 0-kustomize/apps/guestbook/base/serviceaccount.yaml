# Dedicated SA, no RoleBindings attached. The deployment uses this SA and
# disables token automount — guestbook doesn't need to talk to the API
# server, so an RCE in the container has no baseline cluster identity to
# climb from.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: guestbook
automountServiceAccountToken: false
