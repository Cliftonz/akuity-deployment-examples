apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-contact-excluded
  namespace: team-contact-non-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: team-contact-excluded
  template:
    metadata:
      labels:
        app: team-contact-excluded
    spec:
      containers:
        - name: app
          image: nginx:1.25
          ports:
            - containerPort: 80
