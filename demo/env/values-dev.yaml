namespace: guestbook-demo-dev

replicaCount: 1

# Kargo's yaml-update step rewrites imageDigest at promotion time.
# Flat top-level key — yaml-update doesn't traverse nested maps.
imageDigest: ""

httpRoute:
  hostname: guestbook-demo-dev.local
