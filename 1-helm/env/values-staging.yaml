namespace: guestbook-staging

replicaCount: 2

image:
  tag: "1.0"

httpRoute:
  hostname: guestbook-staging.local

# Canary at staging — quick validation cycle, three weight bumps.
rollout:
  enabled: true
