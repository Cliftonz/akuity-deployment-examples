apiVersion: infra.k8/v1alpha1
kind: XResourceQuota
metadata:
  name: external-secrets-quota
spec:
  name: external-secrets-quota
  namespace: external-secrets
  tier: dev
  cluster: demo
  owner: platform-team
  hard:
    pods: "10"
