apiVersion: infra.k8/v1alpha1
kind: XResourceQuota
metadata:
  name: metallb-quota
spec:
  name: metallb-quota
  namespace: metallb-system
  tier: dev
  cluster: demo
  owner: platform-team
  hard:
    pods: "10"
