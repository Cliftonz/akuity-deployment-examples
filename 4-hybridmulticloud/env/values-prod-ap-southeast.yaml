namespace: guestbook-prod

replicaCount: 3

image:
  tag: "0.0.1"

database:
  secretName: guestbook-yugabyte-prod

# ap-southeast lands on the on-prem Composition via the regional seed's
# EnvironmentConfig. Local Postgres on the seed cluster's persistent
# storage; no hyperscaler involvement.

httpRoute:
  hostname: guestbook-prod-ap-southeast.example.com
