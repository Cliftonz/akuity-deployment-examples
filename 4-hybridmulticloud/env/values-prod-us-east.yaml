namespace: guestbook-prod

replicaCount: 3

image:
  tag: "0.0.1"

database:
  secretName: guestbook-yugabyte-prod

# us-east lands on AWS via the regional seed's EnvironmentConfig.
# The values file is identical in shape to other regions — the platform
# layer below the chart routes to the right Composition.
